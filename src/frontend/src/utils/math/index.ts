/**
 * Math utilities — ALL financial and operational calculations live here.
 * GOLDEN RULE: Never import business logic into React components directly.
 * Sprint 1: stub functions — real implementations added in future sprints.
 */

/**
 * Calculate gross margin percentage.
 * @param revenue - Total revenue
 * @param costOfGoods - Cost of goods sold
 */
// TODO: implement margin calculation
export function calculateMargin(
  _revenue: number,
  _costOfGoods: number,
): number {
  return 0;
}

/**
 * Calculate EBITDA (Earnings Before Interest, Taxes, Depreciation, and Amortization).
 * @param revenue - Total revenue
 * @param operatingExpenses - Total operating expenses (excluding ITDA)
 */
// TODO: implement EBITDA calculation
export function calculateEBITDA(
  _revenue: number,
  _operatingExpenses: number,
): number {
  return 0;
}

/**
 * Calculate straight-line depreciation annual amount.
 * @param assetCost - Original asset cost
 * @param residualValue - Expected residual value at end of life
 * @param usefulLifeYears - Useful life of the asset in years
 */
// TODO: implement depreciation calculation
export function calculateDepreciation(
  _assetCost: number,
  _residualValue: number,
  _usefulLifeYears: number,
): number {
  return 0;
}
